1. ОПРЕДЕЛЕНИЕ ИСХОДНОГО МНОЖЕСТВА
Входящий массив данных:
D = {(n₁, c₁, s₁), (n₂, c₂, s₂), ..., (nₖ, cₖ, sₖ)}
где:

nᵢ = номенклатура товара i
cᵢ = категория товара i
sᵢ = годовые продажи товара i

2. ФУНКЦИЯ ОЧИСТКИ ДАННЫХ
Преобразование продаж:
sᵢ = {
  numeric(sᵢ),  если sᵢ конвертируется в число и sᵢ ≥ 0
  0,            если sᵢ = NULL или NaN или пустая ячейка или sᵢ < 0
}
Обработка категорий:
cᵢ = {
  cᵢ,                если cᵢ ≠ NULL и cᵢ ≠ пустая строка
  "Без категории",   если cᵢ = NULL или пустая строка
}

3. БАЗОВЫЕ РАСЧЕТЫ
Общая сумма продаж (только положительные значения):
S_total = Σ(sᵢ) для всех i, где sᵢ > 0
Процент продаж каждого товара:
pᵢ = {
  (sᵢ / S_total) × 100%,  если sᵢ > 0
  0%,                     если sᵢ = 0
}
Сортировка по убыванию продаж:
Товары упорядочиваются: s₁ ≥ s₂ ≥ s₃ ≥ ... ≥ sₖ
Накопительный процент (правило Парето):
C₁ = p₁
C₂ = p₁ + p₂  
C₃ = p₁ + p₂ + p₃
...
Cᵢ = Σ(pⱼ) для j = 1 до i, где sⱼ > 0
Для товаров с нулевыми продажами: Cᵢ = 100.0

4. ФУНКЦИЯ КЛАССИФИКАЦИИ ABC
Математическое правило присвоения класса:
ABC(Cᵢ, sᵢ) = {
  'A',  если sᵢ > 0 И Cᵢ ≤ 80.0
  'B',  если sᵢ > 0 И 80.0 < Cᵢ ≤ 95.0  
  'C',  если sᵢ > 0 И Cᵢ > 95.0 ИЛИ sᵢ = 0
}