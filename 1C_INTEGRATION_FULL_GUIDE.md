# Полное руководство по интеграции 1С с webhook системой

## 📋 Спецификация JSON файлов

### 1. Файл продаж: `sales_YYYY-MM-DD_YYYY-MM-DD.json`

**ВАЖНО**: Имя файла должно содержать период (начало_конец)

**Структура (массив объектов по филиалам):**

```json
[
  {
    "ДатаВыгрузки": "2025-07-17T18:19:48",
    "НачалоПериода": "2025-06-01",
    "КонецПериода": "2025-06-30",
    "Филиал": "4 Склад фурнитуры АЗМ Шымкент \"Овощная база\"",
    "ПродажиПоДням": {
      "2025-06-01": [
        {
          "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
          "Номенклатура": "Плинтус пластик 3м Черный глянец AP740 TP",
          "Количество": 5,
          "Выручка": 16825,
          "Себестоимость": 12430.52,
          "ВаловаяПрибыль": 4394.48,
          "Рентабельность": 26.13,
          "ЕдиницаИзмерения": "Штука",
          "Артикул": "AP740.1905F3",
          "Производитель": "Польша"
        }
      ],
      "2025-06-02": [],  // Пустой массив если нет продаж
      "2025-06-03": [],
      // ... ВСЕ дни периода должны быть включены
    },
    "ИтогиЗаПериод": [
      {
        "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
        "Номенклатура": "Плинтус пластик 3м Черный глянец AP740 TP",
        "ОбщееКоличество": 15,
        "ОбщаяВыручка": 50475,
        "ОбщаяСебестоимость": 37277.48,  // ВАЖНО для расчетов!
        "ОбщаяПрибыль": 13197.52,
        "СредняяРентабельность": 26.13,
        "ЕдиницаИзмерения": "Штука",
        "Артикул": "AP740.1905F3",
        "Производитель": "Польша"
      }
    ]
  },
  // ... другие филиалы
]
```

### 2. Файл остатков: `stock_YYYY-MM-DD.json`

**ВАЖНО**: Имя файла должно содержать дату остатков

**Структура (объект с массивом складов):**

```json
{
  "ДатаОстатков": "2025-06-30T23:59:59",
  "ДатаВыгрузки": "2025-07-20T16:56:12",
  "ОстаткиПоСкладам": [
    {
      "Склад": "4 Склад фурнитуры АЗМ Шымкент \"Овощная база\"",
      "Город": "",
      "Остатки": [
        {
          "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
          "Номенклатура": "Плинтус пластик 3м Кастилло темный AP740 TP",
          "Количество": 127,
          "Стоимость": 314963.33,  // Себестоимость остатков - ВАЖНО!
          "СредняяЦена": "2 480,03",
          "ЕдиницаИзмерения": "Штука",
          "Артикул": "AP740.1242F3",
          "Производитель": "Польша"
        }
      ]
    }
  ]
}
```

## 🔑 Ключевые моменты

### Для файла продаж:
1. **Массив филиалов** - каждый элемент это данные одного филиала
2. **ПродажиПоДням** - объект с ключами-датами, ВСЕ дни периода обязательны
3. **ИтогиЗаПериод** - суммарные данные по каждому товару за период
4. **ОбщаяСебестоимость** - критически важно для расчетов оборачиваемости

### Для файла остатков:
1. **Стоимость** - это себестоимость остатков (не розничная цена!)
2. **СредняяЦена** - строка с пробелами и запятыми

## 🏢 Список филиалов

Точные названия (регистр важен!):
- 4 Склад фурнитуры АЗМ Шымкент "Овощная база"
- 6 Склад фурнитуры "Овощная база" Магазин продажи
- АО Склад Фурнитура TRADE
- Барыс Склад Фурнитура TRADE
- Казыбаева Склад Фурнитура TRADE
- Магазин фурнитуры
- ТД Казыбаева ФУРНИТУРА магазин
- склад фурнитура №1
- База Склад Фурнитура Комплект

## 🔐 Настройка webhook

### Параметры подключения:
```
URL сервера: http://your-server.com:5000
Секретный ключ: your_secret_key_123456
```

### Endpoints:
- `POST /webhook/sales` - отправка продаж
- `POST /webhook/stock` - отправка остатков
- `GET /webhook/status` - проверка статуса
- `GET /webhook/files` - список файлов

### Заголовки запроса:
```
Content-Type: application/json; charset=utf-8
X-Hub-Signature-256: sha256=<подпись>
```

## 💻 Пример кода для 1С

```1c
// Функция создания HMAC-SHA256 подписи
Функция СоздатьПодписьHMAC(Данные, Ключ)
    // Реализация зависит от версии 1С
    // Для 8.3.12+ можно использовать встроенные функции
    // Для старых версий - внешние компоненты
КонецФункции

// Функция отправки данных на webhook
Функция ОтправитьНаWebhook(Endpoint, ДанныеJSON, СекретныйКлюч)
    
    URL = "http://your-server.com:5000" + Endpoint;
    
    // Создаем подпись
    Подпись = "sha256=" + СоздатьПодписьHMAC(ДанныеJSON, СекретныйКлюч);
    
    // Настраиваем HTTP соединение
    Соединение = Новый HTTPСоединение("your-server.com", 5000);
    Запрос = Новый HTTPЗапрос(Endpoint);
    
    // Заголовки
    Запрос.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    Запрос.Заголовки.Вставить("X-Hub-Signature-256", Подпись);
    
    // Тело запроса
    Запрос.УстановитьТелоИзСтроки(ДанныеJSON, КодировкаТекста.UTF8);
    
    // Отправляем
    Попытка
        Ответ = Соединение.ОтправитьДляОбработки(Запрос);
        
        Если Ответ.КодСостояния = 200 Тогда
            Возврат Истина;
        Иначе
            ОбщегоНазначения.СообщитьПользователю(
                "Ошибка webhook: " + Ответ.КодСостояния
            );
            Возврат Ложь;
        КонецЕсли;
        
    Исключение
        ОбщегоНазначения.СообщитьПользователю(
            "Ошибка соединения: " + ОписаниеОшибки()
        );
        Возврат Ложь;
    КонецПопытки;
    
КонецФункции

// Процедура формирования и отправки данных продаж
Процедура ОтправитьПродажиНаWebhook() Экспорт
    
    НачалоПериода = НачалоМесяца(ТекущаяДата());
    КонецПериода = КонецМесяца(ТекущаяДата());
    
    // Формируем массив данных по филиалам
    МассивФилиалов = Новый Массив;
    
    // Запрос для получения филиалов
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ Склад ИЗ РегистрНакопления.ТоварыНаСкладах";
    
    Филиалы = Запрос.Выполнить().Выгрузить();
    
    Для Каждого СтрФилиал Из Филиалы Цикл
        
        ДанныеФилиала = Новый Структура;
        ДанныеФилиала.Вставить("ДатаВыгрузки", ТекущаяДата());
        ДанныеФилиала.Вставить("НачалоПериода", Формат(НачалоПериода, "ДФ=yyyy-MM-dd"));
        ДанныеФилиала.Вставить("КонецПериода", Формат(КонецПериода, "ДФ=yyyy-MM-dd"));
        ДанныеФилиала.Вставить("Филиал", СтрФилиал.Склад.Наименование);
        
        // Формируем продажи по дням
        ПродажиПоДням = Новый Соответствие;
        
        ТекДата = НачалоПериода;
        Пока ТекДата <= КонецПериода Цикл
            
            // Запрос продаж за день
            ПродажиДня = ПолучитьПродажиЗаДень(СтрФилиал.Склад, ТекДата);
            ПродажиПоДням.Вставить(Формат(ТекДата, "ДФ=yyyy-MM-dd"), ПродажиДня);
            
            ТекДата = ТекДата + 86400; // +1 день
        КонецЦикла;
        
        ДанныеФилиала.Вставить("ПродажиПоДням", ПродажиПоДням);
        
        // Формируем итоги за период
        ИтогиЗаПериод = ПолучитьИтогиПродаж(СтрФилиал.Склад, НачалоПериода, КонецПериода);
        ДанныеФилиала.Вставить("ИтогиЗаПериод", ИтогиЗаПериод);
        
        МассивФилиалов.Добавить(ДанныеФилиала);
        
    КонецЦикла;
    
    // Преобразуем в JSON
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, МассивФилиалов);
    СтрокаJSON = ЗаписьJSON.Закрыть();
    
    // Отправляем
    СекретныйКлюч = ПолучитьСекретныйКлючWebhook();
    ОтправитьНаWebhook("/webhook/sales", СтрокаJSON, СекретныйКлюч);
    
КонецПроцедуры
```

## 📅 Настройка регламентных заданий

### В 1С создайте регламентные задания:

1. **Ежедневная выгрузка продаж**
   - Время: 08:00
   - Процедура: `ОтправитьПродажиНаWebhook()`
   - Период: за предыдущий день

2. **Ежедневная выгрузка остатков**
   - Время: 08:30
   - Процедура: `ОтправитьОстаткиНаWebhook()`
   - На дату: конец предыдущего дня

## ✅ Проверка работы

1. **Тестовая отправка:**
   ```bash
   python test_webhook.py
   ```

2. **Проверка логов:**
   ```bash
   tail -f webhook.log
   ```

3. **Проверка файлов:**
   ```
   GET http://your-server.com:5000/webhook/files
   ```

## 🚨 Частые ошибки

1. **401 Unauthorized** - неверная подпись
   - Проверьте секретный ключ
   - Убедитесь что подпись правильно формируется

2. **400 Bad Request** - неверная структура
   - Проверьте все обязательные поля
   - Убедитесь что даты в правильном формате

3. **500 Internal Server Error** - ошибка сервера
   - Проверьте логи webhook.log
   - Убедитесь что директория webhook_uploads доступна для записи

## 📞 Контакты для поддержки

При возникновении проблем предоставьте:
1. Скриншот ошибки из 1С
2. Лог файл webhook.log
3. Пример отправляемых данных (JSON)