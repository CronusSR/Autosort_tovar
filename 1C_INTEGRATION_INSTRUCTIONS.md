# 📋 ИНСТРУКЦИЯ ДЛЯ 1С ПРОГРАММИСТА

## 🎯 ЗАДАЧА
Настроить автоматическую отправку данных из 1С на сервер аналитики.

## 🔗 АДРЕСА СЕРВЕРА

**Основной сервер:** `217.114.1.117`

### Эндпоинты для отправки данных:
- **Продажи:** `http://217.114.1.117:5000/webhook/sales`
- **Остатки:** `http://217.114.1.117:5000/webhook/stock`
- **Проверка работы:** `http://217.114.1.117:5000/webhook/status`

## 🔐 БЕЗОПАСНОСТЬ

**Секретный ключ:** `furniture_company_secret_key_2025`

Каждый запрос должен содержать подпись HMAC-SHA256 в заголовке:
```
X-Hub-Signature-256: sha256={подпись}
```

## 📊 ФОРМАТ ДАННЫХ

### 1. Данные о продажах (POST /webhook/sales)

```json
[
  {
    "ВерсияФормата": "2.0",
    "ДатаВыгрузки": "2025-07-24T01:00:00",
    "НачалоПериода": "2025-07-23",
    "КонецПериода": "2025-07-23",
    "Филиал": "Астана - Магазин",
    "ПродажиПоДням": {
      "2025-07-23": [
        {
          "Код": "00001234",
          "Номенклатура": "Стол письменный дубовый",
          "Количество": 2,
          "Сумма": 85000.00,
          "Категория": "Мебель для офиса"
        }
      ]
    },
    "ИтогиЗаПериод": [
      {
        "Код": "00001234",
        "Номенклатура": "Стол письменный дубовый",
        "ОбщееКоличество": 2,
        "ОбщаяСумма": 85000.00
      }
    ]
  }
]
```

### 2. Данные об остатках (POST /webhook/stock)

```json
{
  "ВерсияФормата": "2.0",
  "ДатаОстатков": "2025-07-24T00:00:00",
  "ДатаВыгрузки": "2025-07-24T01:00:00",
  "ОстаткиПоСкладам": [
    {
      "Склад": "Астана - Склад",
      "Остатки": [
        {
          "Код": "00001234",
          "Номенклатура": "Стол письменный дубовый",
          "Количество": 15,
          "Цена": 42500.00
        }
      ]
    }
  ]
}
```

## 💻 ПРИМЕР КОДА ДЛЯ 1С

```1c
// Процедура отправки данных на сервер аналитики
Процедура ОтправитьДанныеНаСерверАналитики(ТипДанных, ДанныеJSON)
    
    // Настройки
    АдресСервера = "217.114.1.117";
    ПортСервера = "5000";
    СекретныйКлюч = "furniture_company_secret_key_2025";
    
    // Определяем эндпоинт
    Если ТипДанных = "Продажи" Тогда
        Эндпоинт = "/webhook/sales";
    ИначеЕсли ТипДанных = "Остатки" Тогда
        Эндпоинт = "/webhook/stock";
    Иначе
        Возврат;
    КонецЕсли;
    
    URL = "http://" + АдресСервера + ":" + ПортСервера + Эндпоинт;
    
    // Создание HTTP-запроса
    HTTPСоединение = Новый HTTPСоединение(АдресСервера, Число(ПортСервера));
    HTTPЗапрос = Новый HTTPЗапрос(Эндпоинт);
    
    // Подготовка данных
    ТелоЗапроса = ДанныеJSON;
    
    // Создание подписи HMAC-SHA256
    Подпись = ВычислитьПодписьHMAC(ТелоЗапроса, СекретныйКлюч);
    
    // Установка заголовков
    HTTPЗапрос.УстановитьТелоИзСтроки(ТелоЗапроса, КодировкаТекста.UTF8);
    HTTPЗапрос.Заголовки.Вставить("Content-Type", "application/json");
    HTTPЗапрос.Заголовки.Вставить("X-Hub-Signature-256", "sha256=" + Подпись);
    
    // Отправка запроса
    Попытка
        HTTPОтвет = HTTPСоединение.ВызватьHTTPМетод("POST", HTTPЗапрос);
        
        Если HTTPОтвет.КодСостояния = 200 Тогда
            Сообщить("Данные успешно отправлены на сервер аналитики");
            // Логирование успешной отправки
            ЗаписатьВЖурнал("Отправка на сервер аналитики", 
                           "Успешно отправлены " + ТипДанных,
                           HTTPОтвет.ПолучитьТелоКакСтроку());
        Иначе
            Сообщить("Ошибка отправки данных: " + HTTPОтвет.КодСостояния);
            ЗаписатьВЖурнал("Отправка на сервер аналитики", 
                           "Ошибка отправки " + ТипДанных + ": " + HTTPОтвет.КодСостояния,
                           HTTPОтвет.ПолучитьТелоКакСтроку());
        КонецЕсли;
        
    Исключение
        Сообщить("Исключение при отправке данных: " + ОписаниеОшибки());
        ЗаписатьВЖурнал("Отправка на сервер аналитики", 
                       "Исключение при отправке " + ТипДанных,
                       ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры

// Функция вычисления HMAC-SHA256
Функция ВычислитьПодписьHMAC(Данные, Ключ)
    
    ХешированиеДанных = Новый ХешированиеДанных(ХешФункция.SHA256);
    
    // Преобразование ключа
    КлючБайты = ПолучитьБайтыИзСтроки(Ключ, КодировкаТекста.UTF8);
    ДанныеБайты = ПолучитьБайтыИзСтроки(Данные, КодировкТекста.UTF8);
    
    // Для упрощения - используем готовую функцию HMAC если есть
    // Или реализуем алгоритм HMAC-SHA256
    
    ХешированиеДанных.Добавить(КлючБайты);
    ХешированиеДанных.Добавить(ДанныеБайты);
    
    Результат = ХешированиеДанных.ХешСумма;
    
    // Преобразование в HEX-строку
    ВозвратитьHEXСтроку(Результат);
    
КонецФункции

// Регламентное задание для ежедневной выгрузки
Процедура ЕжедневнаяВыгрузкаНаСерверАналитики() Экспорт
    
    // Выгрузка продаж за вчера
    ДатаВчера = НачалоДня(ТекущаяДата() - 86400);
    ДанныеПродаж = СформироватьДанныеПродаж(ДатаВчера, ДатаВчера);
    
    Если ДанныеПродаж <> Неопределено Тогда
        JSONПродажи = ЗаписатьJSON(ДанныеПродаж);
        ОтправитьДанныеНаСерверАналитики("Продажи", JSONПродажи);
    КонецЕсли;
    
    // Выгрузка остатков на сегодня
    ДанныеОстатков = СформироватьДанныеОстатков(НачалоДня(ТекущаяДата()));
    
    Если ДанныеОстатков <> Неопределено Тогда
        JSONОстатки = ЗаписатьJSON(ДанныеОстатков);
        ОтправитьДанныеНаСерверАналитики("Остатки", JSONОстатки);
    КонецЕсли;
    
КонецПроцедуры
```

## ⏰ РАСПИСАНИЕ ОТПРАВКИ

**Рекомендуемое время отправки:** Каждый день в **01:00**

1. **01:00** - Отправка данных о продажах за предыдущий день
2. **01:05** - Отправка данных об остатках на текущую дату

## ✅ ПРОВЕРКА РАБОТЫ

### 1. Проверка доступности сервера:
```
GET http://217.114.1.117:5000/webhook/status
```

**Ожидаемый ответ:**
```json
{
  "status": "active",
  "timestamp": "2025-07-24T10:30:00Z",
  "uptime": "2 days, 3 hours",
  "recent_files": ["sales_2025-07-23_2025-07-23.json"]
}
```

### 2. Тестовая отправка:
```
POST http://217.114.1.117:5000/webhook/sales
Content-Type: application/json
X-Hub-Signature-256: sha256={подпись}

[{"тестовые": "данные"}]
```

## 🔧 УСТРАНЕНИЕ ПРОБЛЕМ

### Коды ошибок:

- **200** - Успешно
- **400** - Неверные данные (проверьте формат JSON)
- **401** - Неверная подпись (проверьте секретный ключ)
- **500** - Ошибка сервера (свяжитесь с администратором)

### Если данные не доходят:
1. Проверьте доступность сервера
2. Убедитесь в правильности подписи
3. Проверьте формат JSON
4. Обратитесь к администратору системы

## 📞 КОНТАКТЫ

При возникновении технических вопросов:
- Проверьте статус сервера: `http://217.114.1.117:5000/webhook/status`
- Сохраните логи отправки из 1С
- Обратитесь к администратору с подробным описанием проблемы

---
**Дата:** 2025-07-24  
**Версия:** 1.0