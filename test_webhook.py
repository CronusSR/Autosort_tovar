#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тестовый скрипт для проверки webhook
"""

import requests
import json
import hmac
import hashlib
from datetime import datetime

# Настройки (должны совпадать с .env)
WEBHOOK_URL = "http://localhost:5000"
SECRET_KEY = "your_secret_key_123456"  # Замените на ваш ключ из .env

def create_signature(payload):
    """Создает подпись для webhook"""
    mac = hmac.new(SECRET_KEY.encode(), payload, hashlib.sha256)
    return f"sha256={mac.hexdigest()}"

def test_sales_webhook():
    """Тестирует отправку данных продаж"""
    print("🔄 Тестируем webhook продаж...")
    
    # Тестовые данные продаж - ТОЧНАЯ СТРУКТУРА из ТЗ
    test_data = [
        {
            "ДатаВыгрузки": datetime.now().isoformat(),
            "НачалоПериода": "2025-01-01",
            "КонецПериода": "2025-01-15",
            "Филиал": "4 Склад фурнитуры АЗМ Шымкент \"Овощная база\"",
            "ПродажиПоДням": {
                "2025-01-01": [],
                "2025-01-02": [
                    {
                        "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
                        "Номенклатура": "Плинтус пластик 3м Черный глянец AP740 TP",
                        "Количество": 5,
                        "Выручка": 16825,
                        "Себестоимость": 12430.52,
                        "ВаловаяПрибыль": 4394.48,
                        "Рентабельность": 26.13,
                        "ЕдиницаИзмерения": "Штука",
                        "Артикул": "AP740.1905F3",
                        "Производитель": "Польша"
                    }
                ],
                "2025-01-03": [],
                "2025-01-04": [],
                "2025-01-05": [
                    {
                        "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
                        "Номенклатура": "Плинтус пластик 3м Черный глянец AP740 TP",
                        "Количество": 7,
                        "Выручка": 23555,
                        "Себестоимость": 17398.72,
                        "ВаловаяПрибыль": 6156.28,
                        "Рентабельность": 26.13,
                        "ЕдиницаИзмерения": "Штука",
                        "Артикул": "AP740.1905F3",
                        "Производитель": "Польша"
                    }
                ]
            },
            "ИтогиЗаПериод": [
                {
                    "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
                    "Номенклатура": "Плинтус пластик 3м Черный глянец AP740 TP",
                    "ОбщееКоличество": 12,
                    "ОбщаяВыручка": 40380,
                    "ОбщаяСебестоимость": 29829.24,
                    "ОбщаяПрибыль": 10550.76,
                    "СредняяРентабельность": 26.13,
                    "ЕдиницаИзмерения": "Штука",
                    "Артикул": "AP740.1905F3",
                    "Производитель": "Польша"
                }
            ]
        },
        {
            "ДатаВыгрузки": datetime.now().isoformat(),
            "НачалоПериода": "2025-01-01",
            "КонецПериода": "2025-01-15",
            "Филиал": "6 Склад фурнитуры \"Овощная база\" Магазин продажи",
            "ПродажиПоДням": {
                "2025-01-01": [],
                "2025-01-02": [],
                "2025-01-03": [
                    {
                        "ПутьКатегорий": "Ручки мебельные/Мебельная фурнитура/",
                        "Номенклатура": "Ручка мебельная 128мм хром",
                        "Количество": 5,
                        "Выручка": 12500,
                        "Себестоимость": 9000,
                        "ВаловаяПрибыль": 3500,
                        "Рентабельность": 28.0,
                        "ЕдиницаИзмерения": "Штука",
                        "Артикул": "H128CR",
                        "Производитель": "Турция"
                    }
                ]
            },
            "ИтогиЗаПериод": [
                {
                    "ПутьКатегорий": "Ручки мебельные/Мебельная фурнитура/",
                    "Номенклатура": "Ручка мебельная 128мм хром",
                    "ОбщееКоличество": 5,
                    "ОбщаяВыручка": 12500,
                    "ОбщаяСебестоимость": 9000,
                    "ОбщаяПрибыль": 3500,
                    "СредняяРентабельность": 28.0,
                    "ЕдиницаИзмерения": "Штука",
                    "Артикул": "H128CR",
                    "Производитель": "Турция"
                }
            ]
        }
    ]
    
    # Преобразуем в JSON
    payload = json.dumps(test_data, ensure_ascii=False).encode('utf-8')
    
    # Создаем подпись
    signature = create_signature(payload)
    
    # Отправляем запрос
    headers = {
        'Content-Type': 'application/json',
        'X-Hub-Signature-256': signature
    }
    
    try:
        response = requests.post(
            f"{WEBHOOK_URL}/webhook/sales",
            data=payload,
            headers=headers
        )
        
        if response.status_code == 200:
            print("✅ Успешно! Ответ сервера:")
            print(json.dumps(response.json(), indent=2, ensure_ascii=False))
        else:
            print(f"❌ Ошибка: {response.status_code}")
            print(response.text)
    
    except requests.exceptions.ConnectionError:
        print("❌ Не удается подключиться к webhook серверу")
        print("   Убедитесь что сервер запущен: python webhook_receiver.py")
    except Exception as e:
        print(f"❌ Ошибка: {e}")

def test_stock_webhook():
    """Тестирует отправку данных остатков"""
    print("\n🔄 Тестируем webhook остатков...")
    
    # Тестовые данные остатков - ТОЧНАЯ СТРУКТУРА из ТЗ
    test_data = {
        "ДатаОстатков": "2025-01-15T23:59:59",
        "ДатаВыгрузки": datetime.now().isoformat(),
        "ОстаткиПоСкладам": [
            {
                "Склад": "4 Склад фурнитуры АЗМ Шымкент \"Овощная база\"",
                "Город": "",
                "Остатки": [
                    {
                        "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
                        "Номенклатура": "Плинтус пластик 3м Кастилло темный AP740 TP",
                        "Количество": 127,
                        "Стоимость": 314963.33,
                        "СредняяЦена": "2 480,03",
                        "ЕдиницаИзмерения": "Штука",
                        "Артикул": "AP740.1242F3",
                        "Производитель": "Польша"
                    },
                    {
                        "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
                        "Номенклатура": "Плинтус пластик 3м Черное серебро AP740 TP",
                        "Количество": 123,
                        "Стоимость": 305315.67,
                        "СредняяЦена": "2 482,24",
                        "ЕдиницаИзмерения": "Штука",
                        "Артикул": "AP740.1276F3",
                        "Производитель": "Польша"
                    }
                ]
            },
            {
                "Склад": "6 Склад фурнитуры \"Овощная база\" Магазин продажи",
                "Город": "",
                "Остатки": [
                    {
                        "ПутьКатегорий": "Ручки мебельные/Мебельная фурнитура/",
                        "Номенклатура": "Ручка мебельная 96мм матовая",
                        "Количество": 45,
                        "Стоимость": 89250.0,
                        "СредняяЦена": "1 983,33",
                        "ЕдиницаИзмерения": "Штука",
                        "Артикул": "H96MT",
                        "Производитель": "Турция"
                    }
                ]
            }
        ]
    }
    
    # Преобразуем в JSON
    payload = json.dumps(test_data, ensure_ascii=False).encode('utf-8')
    
    # Создаем подпись
    signature = create_signature(payload)
    
    # Отправляем запрос
    headers = {
        'Content-Type': 'application/json',
        'X-Hub-Signature-256': signature
    }
    
    try:
        response = requests.post(
            f"{WEBHOOK_URL}/webhook/stock",
            data=payload,
            headers=headers
        )
        
        if response.status_code == 200:
            print("✅ Успешно! Ответ сервера:")
            print(json.dumps(response.json(), indent=2, ensure_ascii=False))
        else:
            print(f"❌ Ошибка: {response.status_code}")
            print(response.text)
    
    except requests.exceptions.ConnectionError:
        print("❌ Не удается подключиться к webhook серверу")
        print("   Убедитесь что сервер запущен: python webhook_receiver.py")
    except Exception as e:
        print(f"❌ Ошибка: {e}")

def check_webhook_status():
    """Проверяет статус webhook сервера"""
    print("🔍 Проверяем статус webhook сервера...")
    
    try:
        response = requests.get(f"{WEBHOOK_URL}/webhook/status")
        
        if response.status_code == 200:
            print("✅ Webhook сервер работает!")
            print(json.dumps(response.json(), indent=2, ensure_ascii=False))
        else:
            print(f"❌ Ошибка: {response.status_code}")
    
    except requests.exceptions.ConnectionError:
        print("❌ Webhook сервер не доступен")
        print("   Запустите: python webhook_receiver.py")
    except Exception as e:
        print(f"❌ Ошибка: {e}")

if __name__ == "__main__":
    print("=== ТЕСТ WEBHOOK СИСТЕМЫ ===\n")
    
    # Проверяем статус
    check_webhook_status()
    
    # Тестируем endpoints
    test_sales_webhook()
    test_stock_webhook()
    
    print("\n=== ТЕСТ ЗАВЕРШЕН ===")
    print("\nЕсли тесты прошли успешно:")
    print("1. Передайте программисту 1С файл: 1c_webhook_example.txt")
    print("2. Сообщите ему URL вашего сервера и секретный ключ")
    print("3. Настройте автоматические отчеты: python automated_report_system.py")