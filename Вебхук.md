# 📋 Руководство по интеграции 1C с вебхуком системы анализа

## 🌐 Основная информация

**URL вебхука:** `http://217.114.1.117:5000`  
**Веб-интерфейс:** `http://217.114.1.117:8502`  
**Протокол:** HTTP POST  
**Формат данных:** JSON  

📁 **Структура папок:**
- Продажи сохраняются в: `webhook_uploads/sales/`
- Остатки сохраняются в: `webhook_uploads/stock/`
- Архив данных: `webhook_uploads/archive/`

---

## 📊 Структура данных для отправки

### 1. Данные о продажах (Sales Data)

**Endpoint:** `POST http://217.114.1.117:5000/sales`
**Папка сохранения:** `webhook_uploads/sales/`

**Структура JSON:**
```json
[
  {
    "ДатаВыгрузки": "2025-07-25T15:30:18",
    "НачалоПериода": "2024-01-01", 
    "КонецПериода": "2024-01-31",
    "Филиал": "Название филиала",
    "Продажи": [
      {
        "День": "2024-01-08",
        "ПродажиПоДням": [
          {
            "ПутьКатегорий": "Конкретная категория/Подкатегория/Основная категория/Мебельная фурнитура/",
            "Номенклатура": "Наименование товара",
            "Количество": 92,
            "Выручка": 9568,
            "Себестоимость": 6253.73,
            "ВаловаяПрибыль": 3314.27,
            "Рентабельность": 34.64,
            "ЕдиницаИзмерения": "Штука",
            "Артикул": "2001/2б",
            "Производитель": "Турция"
          }
        ]
      }
    ]
  }
]
```

### 2. Данные об остатках (Stock Data)

**Endpoint:** `POST http://217.114.1.117:5000/stock`
**Папка сохранения:** `webhook_uploads/stock/`

**Структура JSON:**
```json
{
  "ДатаОстатков": "2025-06-30T23:59:59",
  "ДатаВыгрузки": "2025-07-20T16:56:12",
  "ОстаткиПоСкладам": [
    {
      "Склад": "4 Склад фурнитуры АЗМ Шымкент \"Овощная база\"",
      "Город": "",
      "Остатки": [
        {
          "ПутьКатегорий": "Плинтус пластиковый 3м/Аксессуары для столешниц/Мебельная фурнитура/",
          "Номенклатура": "Плинтус пластик 3м Кастилло темный AP740 TP",
          "Количество": 127,
          "Стоимость": 314963.33,
          "СредняяЦена": "2 480,03",
          "ЕдиницаИзмерения": "Штука",
          "Артикул": "AP740.1242F3",
          "Производитель": "Польша"
        }
      ]
    }
  ]
}
```

---

## 🔧 Настройка в 1C

### 1. Создание HTTP-запроса в 1C

```bsl
// Процедура отправки данных о продажах
Процедура ОтправитьДанныеПродаж(ДанныеJSON)
    
    HTTPСоединение = Новый HTTPСоединение("217.114.1.117", 5000);
    HTTPЗапрос = Новый HTTPЗапрос("/sales");
    
    // Устанавливаем заголовки
    HTTPЗапрос.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    HTTPЗапрос.Заголовки.Вставить("User-Agent", "1C:Enterprise/webhook-client");
    
    // Устанавливаем тело запроса
    HTTPЗапрос.УстановитьТелоИзСтроки(ДанныеJSON, КодировкаТекста.UTF8);
    
    Попытка
        HTTPОтвет = HTTPСоединение.ВыполнитьЗапрос(HTTPЗапрос);
        
        Если HTTPОтвет.КодСостояния = 200 Тогда
            Сообщить("✅ Данные о продажах успешно отправлены");
        Иначе
            Сообщить("❌ Ошибка отправки: " + HTTPОтвет.КодСостояния + " - " + HTTPОтвет.ПолучитьТелоКакСтроку());
        КонецЕсли;
        
    Исключение
        Сообщить("❌ Ошибка соединения: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры

// Процедура отправки данных об остатках
Процедура ОтправитьДанныеОстатков(ДанныеJSON)
    
    HTTPСоединение = Новый HTTPСоединение("217.114.1.117", 5000);
    HTTPЗапрос = Новый HTTPЗапрос("/stock");
    
    HTTPЗапрос.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    HTTPЗапрос.Заголовки.Вставить("User-Agent", "1C:Enterprise/webhook-client");
    
    HTTPЗапрос.УстановитьТелоИзСтроки(ДанныеJSON, КодировкаТекста.UTF8);
    
    Попытка
        HTTPОтвет = HTTPСоединение.ВыполнитьЗапрос(HTTPЗапрос);
        
        Если HTTPОтвет.КодСостояния = 200 Тогда
            Сообщить("✅ Данные об остатках успешно отправлены");
        Иначе
            Сообщить("❌ Ошибка отправки: " + HTTPОтвет.КодСостояния);
        КонецЕсли;
        
    Исключение
        Сообщить("❌ Ошибка соединения: " + ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры
```

### 2. Формирование JSON данных остатков

```bsl
// Функция формирования JSON для остатков
Функция СформироватьJSONОстатков(ДатаОстатков)
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    
    ЗаписьJSON.ЗаписатьНачалоОбъекта();
    
    // Заголовок
    ЗаписьJSON.ЗаписатьИмяСвойства("ДатаОстатков");
    ЗаписьJSON.ЗаписатьЗначение(Формат(ДатаОстатков, "ДФ='yyyy-MM-ddTHH:mm:ss'"));
    
    ЗаписьJSON.ЗаписатьИмяСвойства("ДатаВыгрузки");
    ЗаписьJSON.ЗаписатьЗначение(ТекущаяДатаСеанса());
    
    // Остатки по складам
    ЗаписьJSON.ЗаписатьИмяСвойства("ОстаткиПоСкладам");
    ЗаписьJSON.ЗаписатьНачалоМассива();
    
    // Запрос остатков по складам
    Запрос = Новый Запрос;
    Запрос.Текст = "
        |ВЫБРАТЬ РАЗЛИЧНЫЕ
        |    Остатки.Склад КАК Склад,
        |    Остатки.Склад.Город КАК Город
        |ИЗ
        |    РегистрНакопления.ТоварыНаСкладах КАК Остатки
        |ГДЕ
        |    Остатки.КоличествоОстаток > 0
    ";
    
    СкладыРезультат = Запрос.Выполнить();
    Склады = СкладыРезультат.Выгрузить();
    
    Для Каждого СтрокаСклад Из Склады Цикл
        
        ЗаписьJSON.ЗаписатьНачалоОбъекта();
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Склад");
        ЗаписьJSON.ЗаписатьЗначение(Строка(СтрокаСклад.Склад));
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Город");
        ЗаписьJSON.ЗаписатьЗначение(Строка(СтрокаСклад.Город));
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Остатки");
        ЗаписьJSON.ЗаписатьНачалоМассива();
        
        // Запрос остатков по товарам
        ЗапросТовары = Новый Запрос;
        ЗапросТовары.Текст = "
            |ВЫБРАТЬ
            |    Остатки.Номенклатура.ПутьКатегорий КАК ПутьКатегорий,
            |    Остатки.Номенклатура.Наименование КАК Номенклатура,
            |    Остатки.КоличествоОстаток КАК Количество,
            |    Остатки.СтоимостьОстаток КАК Стоимость,
            |    Остатки.Номенклатура.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
            |    Остатки.Номенклатура.Артикул КАК Артикул,
            |    Остатки.Номенклатура.Производитель КАК Производитель
            |ИЗ
            |    РегистрНакопления.ТоварыНаСкладах.Остатки(&ДатаОстатков, Склад = &Склад) КАК Остатки
            |ГДЕ
            |    Остатки.КоличествоОстаток > 0
            |УПОРЯДОЧИТЬ ПО
            |    Номенклатура
        ";
        
        ЗапросТовары.УстановитьПараметр("ДатаОстатков", ДатаОстатков);
        ЗапросТовары.УстановитьПараметр("Склад", СтрокаСклад.Склад);
        
        РезультатТовары = ЗапросТовары.Выполнить();
        
        Пока РезультатТовары.Следующий() Цикл
            
            ЗаписьJSON.ЗаписатьНачалоОбъекта();
            
            ЗаписьJSON.ЗаписатьИмяСвойства("ПутьКатегорий");
            ЗаписьJSON.ЗаписатьЗначение(РезультатТовары.ПутьКатегорий);
            
            ЗаписьJSON.ЗаписатьИмяСвойства("Номенклатура");
            ЗаписьJSON.ЗаписатьЗначение(РезультатТовары.Номенклатура);
            
            ЗаписьJSON.ЗаписатьИмяСвойства("Количество");
            ЗаписьJSON.ЗаписатьЗначение(РезультатТовары.Количество);
            
            ЗаписьJSON.ЗаписатьИмяСвойства("Стоимость");
            ЗаписьJSON.ЗаписатьЗначение(РезультатТовары.Стоимость);
            
            СредняяЦена = ?(РезультатТовары.Количество > 0, РезультатТовары.Стоимость / РезультатТовары.Количество, 0);
            ЗаписьJSON.ЗаписатьИмяСвойства("СредняяЦена");
            ЗаписьJSON.ЗаписатьЗначение(Формат(СредняяЦена, "ЧЦ=15; ЧДЦ=2; ЧРД=' '"));
            
            ЗаписьJSON.ЗаписатьИмяСвойства("ЕдиницаИзмерения");
            ЗаписьJSON.ЗаписатьЗначение(Строка(РезультатТовары.ЕдиницаИзмерения));
            
            ЗаписьJSON.ЗаписатьИмяСвойства("Артикул");
            ЗаписьJSON.ЗаписатьЗначение(РезультатТовары.Артикул);
            
            ЗаписьJSON.ЗаписатьИмяСвойства("Производитель");
            ЗаписьJSON.ЗаписатьЗначение(РезультатТовары.Производитель);
            
            ЗаписьJSON.ЗаписатьКонецОбъекта();
            
        КонецЦикла;
        
        ЗаписьJSON.ЗаписатьКонецМассива(); // Остатки
        ЗаписьJSON.ЗаписатьКонецОбъекта(); // Склад
        
    КонецЦикла;
    
    ЗаписьJSON.ЗаписатьКонецМассива(); // ОстаткиПоСкладам
    ЗаписьJSON.ЗаписатьКонецОбъекта(); // основной объект
    
    Возврат ЗаписьJSON.Закрыть();
    
КонецФункции
```

### 3. Формирование JSON данных продаж

```bsl
// Функция формирования JSON для продаж
Функция СформироватьJSONПродаж(НачалоПериода, КонецПериода, Филиал)
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    
    ЗаписьJSON.ЗаписатьНачалоМассива();
    ЗаписьJSON.ЗаписатьНачалоОбъекта();
    
    // Заголовок
    ЗаписьJSON.ЗаписатьИмяСвойства("ДатаВыгрузки");
    ЗаписьJSON.ЗаписатьЗначение(ТекущаяДатаСеанса());
    
    ЗаписьJSON.ЗаписатьИмяСвойства("НачалоПериода");
    ЗаписьJSON.ЗаписатьЗначение(Формат(НачалоПериода, "ДФ=yyyy-MM-dd"));
    
    ЗаписьJSON.ЗаписатьИмяСвойства("КонецПериода");
    ЗаписьJSON.ЗаписатьЗначение(Формат(КонецПериода, "ДФ=yyyy-MM-dd"));
    
    ЗаписьJSON.ЗаписатьИмяСвойства("Филиал");
    ЗаписьJSON.ЗаписатьЗначение(Филиал);
    
    // Продажи
    ЗаписьJSON.ЗаписатьИмяСвойства("Продажи");
    ЗаписьJSON.ЗаписатьНачалоМассива();
    
    // Запрос продаж по дням
    Запрос = Новый Запрос;
    Запрос.Текст = "
         < /dev/null | ВЫБРАТЬ
        |    НАЧАЛОПЕРИОДА(Продажи.Дата, ДЕНЬ) КАК День,
        |    Продажи.Номенклатура.ПутьКатегорий КАК ПутьКатегорий,
        |    Продажи.Номенклатура.Наименование КАК Номенклатура,
        |    СУММА(Продажи.Количество) КАК Количество,
        |    СУММА(Продажи.Сумма) КАК Выручка,
        |    СУММА(Продажи.Себестоимость) КАК Себестоимость,
        |    Продажи.Номенклатура.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
        |    Продажи.Номенклатура.Артикул КАК Артикул,
        |    Продажи.Номенклатура.Производитель КАК Производитель
        |ИЗ
        |    РегистрНакопления.Продажи КАК Продажи
        |ГДЕ
        |    Продажи.Дата МЕЖДУ &НачалоПериода И &КонецПериода
        |    И Продажи.Организация = &Филиал
        |СГРУППИРОВАТЬ ПО
        |    НАЧАЛОПЕРИОДА(Продажи.Дата, ДЕНЬ),
        |    Продажи.Номенклатура
        |УПОРЯДОЧИТЬ ПО
        |    День
    ";
    
    Запрос.УстановитьПараметр("НачалоПериода", НачалоПериода);
    Запрос.УстановитьПараметр("КонецПериода", КонецПериода);
    Запрос.УстановитьПараметр("Филиал", Филиал);
    
    Результат = Запрос.Выполнить();
    
    // Группируем по дням
    ТекущийДень = Неопределено;
    
    Пока Результат.Следующий() Цикл
        
        Если ТекущийДень <> Результат.День Тогда
            
            Если ТекущийДень <> Неопределено Тогда
                ЗаписьJSON.ЗаписатьКонецМассива(); // Закрываем ПродажиПоДням
                ЗаписьJSON.ЗаписатьКонецОбъекта(); // Закрываем день
            КонецЕсли;
            
            ТекущийДень = Результат.День;
            
            ЗаписьJSON.ЗаписатьНачалоОбъекта();
            ЗаписьJSON.ЗаписатьИмяСвойства("День");
            ЗаписьJSON.ЗаписатьЗначение(Формат(ТекущийДень, "ДФ=yyyy-MM-dd"));
            
            ЗаписьJSON.ЗаписатьИмяСвойства("ПродажиПоДням");
            ЗаписьJSON.ЗаписатьНачалоМассива();
            
        КонецЕсли;
        
        // Записываем товар
        ЗаписьJSON.ЗаписатьНачалоОбъекта();
        
        ЗаписьJSON.ЗаписатьИмяСвойства("ПутьКатегорий");
        ЗаписьJSON.ЗаписатьЗначение(Результат.ПутьКатегорий);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Номенклатура");
        ЗаписьJSON.ЗаписатьЗначение(Результат.Номенклатура);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Количество");
        ЗаписьJSON.ЗаписатьЗначение(Результат.Количество);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Выручка");
        ЗаписьJSON.ЗаписатьЗначение(Результат.Выручка);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Себестоимость");
        ЗаписьJSON.ЗаписатьЗначение(Результат.Себестоимость);
        
        ВаловаяПрибыль = Результат.Выручка - Результат.Себестоимость;
        ЗаписьJSON.ЗаписатьИмяСвойства("ВаловаяПрибыль");
        ЗаписьJSON.ЗаписатьЗначение(ВаловаяПрибыль);
        
        Рентабельность = ?(Результат.Выручка > 0, ВаловаяПрибыль / Результат.Выручка * 100, 0);
        ЗаписьJSON.ЗаписатьИмяСвойства("Рентабельность");
        ЗаписьJSON.ЗаписатьЗначение(Рентабельность);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("ЕдиницаИзмерения");
        ЗаписьJSON.ЗаписатьЗначение(Результат.ЕдиницаИзмерения);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Артикул");
        ЗаписьJSON.ЗаписатьЗначение(Результат.Артикул);
        
        ЗаписьJSON.ЗаписатьИмяСвойства("Производитель");
        ЗаписьJSON.ЗаписатьЗначение(Результат.Производитель);
        
        ЗаписьJSON.ЗаписатьКонецОбъекта();
        
    КонецЦикла;
    
    Если ТекущийДень <> Неопределено Тогда
        ЗаписьJSON.ЗаписатьКонецМассива(); // ПродажиПоДням
        ЗаписьJSON.ЗаписатьКонецОбъекта(); // день
    КонецЕсли;
    
    ЗаписьJSON.ЗаписатьКонецМассива(); // Продажи
    ЗаписьJSON.ЗаписатьКонецОбъекта(); // основной объект
    ЗаписьJSON.ЗаписатьКонецМассива(); // корневой массив
    
    Возврат ЗаписьJSON.Закрыть();
    
КонецФункции
```

---

## 📋 Регламентные задания

### 1. Ежедневная отправка данных

```bsl
// Регламентное задание "Отправка данных в систему аналитики"
Процедура ОтправкаДанныхВСистемуАналитики()
    
    // Отправляем продажи за вчера
    ВчерашняяДата = ТекущаяДатаСеанса() - 86400;
    НачалоДня = НачалоДня(ВчерашняяДата);
    КонецДня = КонецДня(ВчерашняяДата);
    
    // Получаем список филиалов
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ Организация ИЗ РегистрНакопления.Продажи";
    Филиалы = Запрос.Выполнить();
    
    Пока Филиалы.Следующий() Цикл
        
        JSONПродаж = СформироватьJSONПродаж(НачалоДня, КонецДня, Филиалы.Организация);
        ОтправитьДанныеПродаж(JSONПродаж);
        
        // Пауза между отправками
        Приостановить(1000);
        
    КонецЦикла;
    
    // Отправляем текущие остатки
    ДатаОстатков = КонецДня(ТекущаяДатаСеанса());
    JSONОстатков = СформироватьJSONОстатков(ДатаОстатков);
    ОтправитьДанныеОстатков(JSONОстатков);
    
КонецПроцедуры
```

### 2. Настройка расписания

- **Время выполнения:** Ежедневно в 01:00
- **Пользователь:** Системный администратор
- **Максимальное время выполнения:** 30 минут

---

## 🔍 Диагностика и мониторинг

### 1. Проверка доступности вебхука

```bsl
Функция ПроверитьДоступностьВебхука()
    
    Попытка
        HTTPСоединение = Новый HTTPСоединение("217.114.1.117", 5000);
        HTTPЗапрос = Новый HTTPЗапрос("/health");
        HTTPОтвет = HTTPСоединение.ВыполнитьЗапрос(HTTPЗапрос);
        
        Возврат HTTPОтвет.КодСостояния = 200;
        
    Исключение
        Возврат Ложь;
    КонецПопытки;
    
КонецФункции
```

### 2. Логирование ошибок

```bsl
Процедура ЗаписатьВЖурналРегистрации(Сообщение, УровеньЛога = "Информация")
    
    ЗаписьЖурналаРегистрации(
        "Интеграция.СистемаАналитики",
        УровеньЛога,
        ,
        ,
        Сообщение
    );
    
КонецПроцедуры
```

---

## ⚠️ Важные требования

### 1. Кодировка и формат
- **Кодировка:** UTF-8
- **Формат даты:** "yyyy-MM-dd" для дат
- **Формат даты-времени:** "yyyy-MM-ddTHH:mm:ss"
- **Десятичный разделитель:** Точка (.)

### 2. Обязательные поля

**Для продаж:**
- ДатаВыгрузки
- НачалоПериода / КонецПериода  
- Филиал
- ПутьКатегорий (ВАЖНО: должен содержать полный путь с "/")
- Номенклатура
- Количество
- Выручка
- Артикул

**Для остатков:**
- ДатаОстатков
- ДатаВыгрузки
- ОстаткиПоСкладам → Склад
- ОстаткиПоСкладам → Остатки → ПутьКатегорий
- ОстаткиПоСкладам → Остатки → Номенклатура
- ОстаткиПоСкладам → Остатки → Количество
- ОстаткиПоСкладам → Остатки → Стоимость
- ОстаткиПоСкладам → Остатки → Артикул

### 3. Структура ПутьКатегорий

**Правильно:**
```
"Конкретная подкатегория/Средняя категория/Основная категория/Мебельная фурнитура/"
```

**Примеры:**
- "19*0,8мм ПВХ/Кромка ПВХ Китай/Кромка ПВХ/Кромочные материалы/Мебельная фурнитура/"
- "Опоры пластиковые/Опоры мебельные/Ручки, крючки, опоры/Мебельная фурнитура/"

---

## 🎯 Результат интеграции

После успешной настройки в веб-интерфейсе системы будут доступны:

1. **Общий анализ** - метрики продаж и остатков
2. **Анализ оборачиваемости** - скорость продаж товаров  
3. **Анализ по городам** - региональная статистика
4. **ABC анализ по категориям** - классификация товаров по доходности
5. **Межфилиальные перемещения** - рекомендации по оптимизации остатков

---

## 📞 Контакты для поддержки

- **Вебхук:** http://217.114.1.117:5000
- **Интерфейс:** http://217.114.1.117:8502
- **Статус системы:** Проверить доступность вебхука через /health endpoint

---

## 🔄 Версионность

**Текущая версия:** 1.0  
**Дата документа:** 25.07.2025  
**Последние изменения:** Исправлена логика обработки категорий в ABC анализе
