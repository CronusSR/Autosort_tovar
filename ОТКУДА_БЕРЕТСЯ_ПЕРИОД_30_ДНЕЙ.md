# ОТКУДА БЕРЕТСЯ ПЕРИОД 30 ДНЕЙ В РАСЧЕТАХ

## 🎯 КРАТКИЙ ОТВЕТ
**Период 30 дней = КОНСТАНТА**, жестко прописанная в коде для стандартизации всех расчетов.

---

## 📍 МЕСТА В КОДЕ ГДЕ ИСПОЛЬЗУЕТСЯ:

### 1. Расчет оборачиваемости:
**Файл:** `turnover_report_generator.py:122-124`
```python
# ОБОР ДН = (Остаток * Период) / Продажи (по себестоимости)
# Период = 30 дней (месяц)
if cat_sales_cost > 0:
    turnover_days = (cat_stock * 30) / cat_sales_cost
```

### 2. Расчет ADS (средних дневных продаж):
**Файлы:** `multi_branch_analyzer.py`, `warehouse_*.py`
```python
# ADS за месяц
monthly_sales = ads_value * 30
months_of_stock = current_stock / (ads_value * 30)
```

### 3. Расчет месячного оборота:
**Файлы:** `complete_*.py`, `simple_price_add.py:76`
```python
# Общая стоимость месячного оборота
total_value = (ads_data['ads'] * 30 * ads_data['last_purchase_price']).sum()
```

---

## 🔍 ГДЕ ФАКТИЧЕСКИ БЕРУТСЯ ДАТЫ

### JSON файлы содержат период:
```json
{
  "НачалоПериода": "2025-06-01",
  "КонецПериода": "2025-06-30",
  "Продажи": [...]
}
```

### Пример фактического периода:
- **2025-06-01** до **2025-06-30** = **30 дней**
- **2025-07-01** до **2025-07-15** = **15 дней**

---

## ⚠️ ВАЖНАЯ ОСОБЕННОСТЬ СИСТЕМЫ

### Система НЕ использует фактические даты!

```python
# ❌ НЕ ДЕЛАЕТСЯ:
actual_period = (end_date - start_date).days
turnover = (stock * actual_period) / sales

# ✅ ДЕЛАЕТСЯ:
turnover = (stock * 30) / sales  # Всегда 30!
```

### Почему так сделано:
1. **Стандартизация** - все отчеты сопоставимы
2. **Упрощение** - не нужно каждый раз парсить даты
3. **Месячная база** - бизнес мыслит месяцами
4. **Историческая причина** - изначально данные были только за месяц

---

## 🎯 ПРАКТИЧЕСКИЕ ПОСЛЕДСТВИЯ

### Если данные за 15 дней:
```
Фактические продажи за 15 дней: 500,000 ₸
Остаток: 1,000,000 ₸

❌ Неправильно (система делает):
Оборачиваемость = (1,000,000 * 30) / 500,000 = 60 дней

✅ Правильно было бы:
Оборачиваемость = (1,000,000 * 15) / 500,000 = 30 дней
```

### Если данные за 45 дней:
```
Фактические продажи за 45 дней: 1,500,000 ₸
Остаток: 1,000,000 ₸

❌ Неправильно (система делает):
Оборачиваемость = (1,000,000 * 30) / 1,500,000 = 20 дней

✅ Правильно было бы:
Оборачиваемость = (1,000,000 * 45) / 1,500,000 = 30 дней
```

---

## 💡 РЕКОМЕНДАЦИИ

### Для точных расчетов нужно:

1. **Парсить даты из JSON:**
```python
start_date = datetime.strptime(data['НачалоПериода'], '%Y-%m-%d')
end_date = datetime.strptime(data['КонецПериода'], '%Y-%m-%d')
actual_days = (end_date - start_date).days
```

2. **Использовать фактический период:**
```python
turnover_days = (stock * actual_days) / sales
```

3. **Или нормализовать к 30 дням:**
```python
normalized_sales = (sales / actual_days) * 30
turnover_days = (stock * 30) / normalized_sales
```

---

## 🏷️ СТАТУС: ОБЛАСТЬ ДЛЯ УЛУЧШЕНИЯ

**Текущий подход:** ❌ Упрощенный (константа 30)
**Желательный подход:** ✅ Динамический (фактические даты)

**Влияние на точность:** 
- При периодах близких к 30 дням: минимальное
- При коротких периодах (7-15 дней): существенное искажение
- При длинных периодах (45-60 дней): умеренное искажение

---

*Обновлено: 2025-07-21*